<form method="post" action="{{ path('app_project_delete', {'id': project.id}) }}" onsubmit="return confirm('Es-tu sûr de vouloir supprimer ce projet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
    <button class="uk-button uk-button-danger uk-button-small uk-border-pill uk-box-shadow-hover-small">Supprimer le projet</button>
</form>
