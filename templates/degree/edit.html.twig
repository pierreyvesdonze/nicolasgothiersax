{% extends 'base.html.twig' %}

{% block title %}Modifier le diplôme
{% endblock %}

{% block body %}
	<h1>Modifier le diplôme</h1>

	{{ include('degree/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

	<div class="action-buttons">
    <a href="{{ path('app_degree_index') }}" 
       class="uk-button uk-button-default uk-border-pill uk-box-shadow-hover-small action-btn">
        Retour à la liste
    </a>

    {{ include('degree/_delete_form.html.twig') }}
</div>

{% endblock %}
